apiVersion: v1
kind: Service
metadata:
  name: spring-kube-service
  labels:
    app: spring-kube
spec:
  selector:
    app: spring-kube
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort